[project]
name = "tinker-clojure-sidecar"
version = "0.1.0"
description = "Python sidecar for Pi's tinker-clojure extension"
requires-python = ">=3.11"
dependencies = [
    "orjson>=3.11.3",
    "python-dotenv==1.2.1",
    "tinker==0.18.1",
    "transformers==5.5.4",
]

[tool.uv]
package = false
